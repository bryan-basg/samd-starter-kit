# requirements-dev.txt — runtime + herramientas de verificación (tests, lint, tipos).
-r requirements.txt
pytest>=8
pytest-asyncio>=0.23
httpx>=0.27          # lo usa fastapi.testclient.TestClient
# Cobertura reproducible en paralelo (el comando canónico de TESTING_TOOLS.md:
# `pytest -n auto --cov ... && coverage combine && coverage report --fail-under=95`).
coverage>=7
pytest-cov>=5
pytest-xdist>=3
greenlet>=3          # requerido por concurrency=greenlet del .coveragerc (backend async real)
ruff
mypy
vulture
